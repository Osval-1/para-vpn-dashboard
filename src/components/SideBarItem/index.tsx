import React from "react";
import { dashboard } from "../../assets";

const SidebarItem = () => {
  return (
    <section>
      <img src={dashboard} alt="" />
      <p>Dashboard</p>
    </section>
  );
};

export default SidebarItem;
